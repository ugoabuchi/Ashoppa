import React from 'react';
import { IconComponentT } from '../types';

//const MenuIcon = ({ theme, actionPressed = () => { } }: IconComponentT) => (<TouchableOpacity onPress={() => actionPressed()}><FontAwesome name="grade" color={theme.GenralComponentColors.BASIC} size={theme.GenralComponentColors.BUTTONICONSIZE5} /></TouchableOpacity>);

//export {};
